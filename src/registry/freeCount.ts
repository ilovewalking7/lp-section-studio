// このファイルは scripts/build-free-ids.mjs により自動生成されます。直接編集しないでください。
// 元データ: mcp/data/components.json（無料版 = MCP に同梱する 100 個）
// 件数だけを使う面（LP のスタットバンド等）はこちらを読む。
// ID 一覧を初期チャンクに載せないためにファイルを分けている。

/** 無料版に含まれるコンポーネント数 */
export const FREE_EDITION_COUNT = 100;
