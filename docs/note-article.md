# 自作コンポーネント880個を全部機械検査にかけたら、109件の欠陥が出てきた

> note 掲載用の原稿。公開前に「事実確認」の節の数字を再実行して照合すること。

---

2年かけて、UIコンポーネントを880個ためました。

React + TypeScript + Tailwind で、1ファイル1コンポーネント。ヒーロー、料金表、フッター、3Dカルーセル、和風テーマ、ブルータリズム。CIも回していて、テストは全部緑でした。

そのテストが見ていたのは、**「描画しても例外が出ないこと」だけ**でした。

そこに検査を4つ足しました。結果、**109件の欠陥**が出てきました。全部、自分で書いて、自分で目視確認して、緑のCIを通してきたものです。

この記事は、その109件が何だったのかという話です。

---

## 足した検査

| 検査 | 何を見るか |
|---|---|
| コントラスト | デザイントークンの色を、WCAG の相対輝度で計算する |
| axe-core | 構造アクセシビリティ（名前のない要素、ARIAの誤用など） |
| キーボード | 対話部品にキーボードで到達できるか |
| 横スクロール | 実ブラウザ・幅375px で、ページが横に破れないか |

4つに分けたのには理由があります。**jsdom には描画エンジンが無いので、色も幅も測れない**からです。axe を jsdom で走らせても `color-contrast` ルールは動きません。だから色はトークン定義そのものを数値で計算し、幅は実ブラウザ（Chromium）で測る。役割を分けないと、検査したつもりの穴ができます。

---

## 出てきたもの

```
コントラスト  12組が基準未達
axe          29コンポーネントに違反
キーボード    0件（これは通った）
横スクロール  26コンポーネントがモバイル幅でページを破る
```

合計109件。以下、印象に残ったものを4つ。

---

### 1. コンポーネントが aria-label を握りつぶしていた

axe が `button-name` 違反を29箇所出しました。「ボタンに名前がない」——アイコンだけのボタンで、スクリーンリーダーが用途を読めない状態です。

そのうち13箇所が `<Switch>` でした。原因を見に行くと、こうなっていました。

```tsx
export interface SwitchProps {
  checked?: boolean;
  onCheckedChange?: (checked: boolean) => void;
  disabled?: boolean;
  className?: string;
  id?: string;
  // aria-label が無い
}
```

中身が空の `<button>` なので、名前は外から与えるしかありません。それができない設計でした。

ここからが本題です。**利用側のコードを見たら、こう書いてありました。**

```tsx
<Switch
  checked={annual}
  onCheckedChange={setAnnual}
  aria-label={en ? "Switch to annual billing" : "年額請求に切り替え"}
/>
```

**渡していたんです。**書いた本人（私）は正しくやろうとしていて、コンポーネント側が黙って捨てていた。

これは使用箇所をいくらレビューしても見つかりません。コードとしては完璧に見えるからです。実際に描画してその結果を検査して初めて出てきました。

---

### 2. ひとつの色が、両立しない2つの仕事をしていた

コントラスト検査で12組の未達が出たので、明度を機械的に解いて直していきました。11組は素直に解けました。

**最後の1組だけが、どうやっても解けませんでした。**

```
[ダーク] NG 破壊的ボタンの文字 4.09:1（必要 4.5:1）
```

`--destructive`（赤）が、ダークモードで2つの役割を兼ねていました。

- **暗い背景の上のエラー文字** → 読ませるには明るくしたい
- **白文字を載せる削除ボタンの背景** → 白が読めるよう暗くしたい

同じ変数で、明るくも暗くもできません。**トークンの設計が間違っていた**という話で、値をいじって解決する問題ではありませんでした。

結局、ダークモードでは前景色のほうを暗色（`0 60% 7.3%`）に変えて両立させました。

この種の衝突は、測って初めて可視化されます。デザインツールの上で色を選んでいる限り、「この赤は2つの役割を持っている」とは気づけません。

---

### 3. 入場アニメーションが、ページを横に破っていた

横スクロール検査で26件が引っかかりました。3Dカルーセルが `overflow` を閉じ忘れている、といった予想通りのものが大半でしたが、2件だけ意外なものがありました。

```tsx
className={cn(
  "transition-all duration-700",
  shown ? "translate-x-0 opacity-100" : "translate-x-8 opacity-0"
)}
```

スクロールで滑り込んでくる、よくある演出です。**滑り込む前は、要素が右に32pxずれています。**

つまり画面に入る前の要素が、その間ずっと画面幅を超えている。実際のページでは、**読み込むたびに横スクロールバーが一瞬出て消えます。**

自分で何十回も見ていたはずの画面でした。一瞬すぎて、目では認識できていませんでした。

---

### 4. OFF のスイッチが、ほぼ見えていなかった

コントラスト検査が `--input` を叩きました。

```
[ライト] NG 入力欄の枠 1.27:1（必要 3:1）
```

WCAG 1.4.11 は「操作できる部品の境界」に 3:1 を要求します。入力欄の枠は「そこが入力欄である」ことを示す唯一の手がかりなので、対象です。1.27:1 では、ほぼ見えていません。

直すために `--input` を 90% → 60.3% に変えたのですが、**このトークンがもう1箇所で使われていました。**

```tsx
on ? "bg-primary" : "bg-input"   // スイッチのOFF時トラック
```

つまり**OFFのスイッチが、背景と 1.27:1 でほとんど見えていなかった**。入力欄の枠を直したら、そちらも同時に直りました。

---

## 直した

全部直しました。

```
コントラスト  12 → 0
axe          29 → 0
キーボード     0 → 0
横スクロール  26 → 0
```

修正は880個中およそ60ファイルに及びました。ほとんどは `aria-label` を足すような機械的なものですが、いくつかは構造を変える必要がありました（`role="button"` のカードの中にボタンがあって入れ子になっていたもの、`<dl>` が仕様上持てない構造だったもの、など）。

そして途中で**別のテストが8件落ちました。**LPビルダーがコンポーネントの描画HTMLを文字列一致で探していて、`aria-label` が増えたぶん一致しなくなったためです。既存のテストが波及を捕まえてくれました。

---

## 残さなかったこと

検査を入れた時点では、当然ながら大量に赤くなります。ここで「一旦 skip」とすると、検査そのものが忘れられます。

なので baseline ファイルに既知分を列挙し、次の2方向で固定しました。

1. **一覧に無いものの違反は即失敗** → 新規混入を止める
2. **一覧にあるのに違反が無くなったものも失敗** → 直したら必ず消す

一覧は減る方向にしか動きません。そして今、両方とも空です。

---

## やってみて思ったこと

**目視は、この種の欠陥に対して無力です。**

1.27:1 のコントラストも、一瞬だけ出る横スクロールバーも、捨てられている `aria-label` も、見て気づけるものではありません。私は2年間これらを見続けていて、1件も気づきませんでした。

同時に、**検査を作るのは思ったより軽い作業でした。**4つ合わせて数百行です。難しいのは検査を書くことではなく、「検査していない」という状態に気づくことのほうでした。CIが緑だと、検査した気になります。

もうひとつ。**検査は、売り文句の精度も上げました。**

このコンポーネント集は「React を使わない現場でも使える」ことを特徴にしようとしていました。静的HTMLとして書き出せるからです。ところが全880件を実際に書き出して数えたら、**そのまま使えるのは397個**でした。残り483個は状態や操作を持つので、静的版は見た目だけになります。

「880個すべてがReact不要」と書きたくなるところでした。測っていなければ、そう書いていたと思います。

---

## 事実確認

この記事の数字は、以下のコマンドで再現できます。

```bash
npm test                  # 描画・コントラスト・a11y・キーボード
npm run check:contrast    # コントラスト（全組み合わせを表示）
npm run check:overflow    # 横スクロール（実ブラウザ・幅375px）
```

---

*このコンポーネント集は、無料のMCPサーバとして100個を配っています。全880個は買い切りです。（※リンクは公開時に差し替え）*
