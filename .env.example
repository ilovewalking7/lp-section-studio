# 環境変数のサンプル。実際の値は Cloudflare Pages の
# Settings → Environment variables に設定する（リポジトリにIDを直書きしない）。

# Microsoft Clarity プロジェクトID（Cookieレスのヒートマップ＋セッション再生）。
# 未設定なら分析は完全に無効。本番ビルド時のみ読み込まれる。
VITE_CLARITY_ID=

# Cloudflare Web Analytics はコード不要（Pages ダッシュボードで自動注入）。
# 詳細は docs/ANALYTICS.md を参照。
