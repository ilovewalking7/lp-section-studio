# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# Build output
dist/
dist-ssr/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
*.log

# Claude Code local (session-specific) settings — keep out of version control
.claude/settings.local.json

# 配信用レジストリ（npm run build / npm run registry で生成）
public/r/

# 静的HTMLは生成物（npm run html で作る）。6.7MB あるので配信物には含めない。
public/html/
